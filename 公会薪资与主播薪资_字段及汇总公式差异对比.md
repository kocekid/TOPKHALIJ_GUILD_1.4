# 公会薪资与主播薪资——线上代码与 1.4 需求差异对比

## 一、核对范围

### 1. 需求基线

- 《公会1.4后台薪资需求文档PRD》第二章“统一计算口径”
- 第三章“后台—主播薪资”
- 第四章“后台—公会薪资”

### 2. 代码范围

- 代码目录：`/Users/mac/Downloads/manage-api-dev-fa44bb157c04124f9ec2288460ac56a546be95b9`
- 主要控制器：`app/Http/Controllers/Admin/GuildController.php`
- 月结工资模型：`app/Models/SociatyDB/GuildSalaryDetail.php`
- 月结账单模型：`app/Models/SociatyDB/GuildSettlementBill.php`
- 周结工资模型：`app/Models/SociatyDB/GuildSalaryDetailWeekly.php`
- 余额池模型：`app/Models/SociatyDB/GuildBalancePool.php`
- 相关表结构：`database/migrations/2026_06_15_000000_create_guild_core_tables.php`

### 3. 核对边界

当前 `manage-api` 主要负责后台查询、汇总、字段映射、导出和调用结算服务。

月度结算生成、确认及实时工资刷新会调用外部 `room-api`：

- `/internal/guild/settlement/monthly`
- `/internal/guild/settlement/confirm`
- `/internal/guild/salary/realtime/refresh`

因此，本报告分为两类结论：

1. **已确认差异**：可直接从 `manage-api` 的查询、汇总、字段返回和余额池逻辑确认。
2. **需联查 room-api**：`manage-api` 只读取计算结果，无法单独证明上游工资计算公式是否正确。

---

## 二、结论摘要

| 编号 | 差异 | 需求口径 | 当前代码 | 结论 | 风险 |
|---|---|---|---|---|---|
| D-01 | 主播、公会长非金币薪资字段 | 必须分别展示主播非金币薪资、公会长非金币分成薪资 | 接口主要返回总薪资和金币薪资，没有完整、独立的两类非金币字段 | 字段缺失，无法直接按 1.4 表头展示 | P0 |
| D-02 | 公会总非金币薪资 | `主播非金币薪资 + 公会长非金币分成薪资` | 代码直接汇总 `non_coin_reward_amount`，未按两类工资项显式相加 | 公式不透明，不能证明符合需求 | P0 |
| D-03 | 公会总薪资 | `主播总薪资 + 公会长总薪资` | 公会页直接读取/汇总公会长汇总行的 `total_salary_amount`，未在查询层显式相加 | 存在字段映射错误风险，可能把管理收益当公会总薪资 | P0 |
| D-04 | 可提现余额（含金币） | 当前实时余额，包含符合提现条件的非金币与金币工资余额 | 主播列表只统计余额池 1、3，明确排除余额池 2 金币工资 | “含金币”口径未实现 | P0 |
| D-05 | 历史周期可提现余额 | 即使查询历史周期，也展示当前实时余额 | 历史周期读取结算快照中的 `withdrawable_amount` | 时间口径相反 | P0 |
| D-06 | 公会可提现余额（含金币） | 当前公会可归因的主播实时余额 + 公会长管理收益实时余额 | 当前返回的是金币奖励余额、工资余额、减预支后的余额等旧字段，没有一个符合新定义的统一字段 | 新字段未实现 | P0 |
| D-07 | 预支业务 | 1.4 已取消预支、借支、待抵扣及对冲计算 | 主播、公会汇总仍查询并扣除 `advance_deduction_amount` | 旧业务仍参与金额计算 | P0 |
| D-08 | 起始/最终钻石余额、公会总薪资余额 | 1.4 明确删除 | 接口汇总、列表、导出仍返回这些字段 | 字段未清理 | P1 |
| D-09 | 金币工资比例 | 每笔工资项按当期政策快照拆分；周结确认比例为 0.2 | 本地常量自由政策为 0.15，普通政策为 0；是否被上游覆盖不明确 | 参数存在冲突，必须联查 room-api | P0 |
| D-10 | 主播工资与公会长收益计算基础 | 主播按有效钻石余额；公会长按有效钻石流水 | 本地遗留计算函数用同一个 `$validDiamond` 同时算主播和公会长 | 本地函数不符合需求；当前主链路是否使用需联查 | P0 |
| D-11 | 周结政策刷新 | 支持自由政策（周结） | 列表支持 `policy_type=3`，但指定成员实时刷新仅接受政策 1、2 | 周结切换后实时刷新存在失败风险 | P1 |
| D-12 | 兑换钻石流水 | 只汇总已确认兑换记录，仅用于对账 | 月结按 `ABS(income)` 汇总，并允许后台覆盖记录优先 | 数据来源需确认是否等同“已确认兑换记录” | P1 |

### 直接结论

当前代码不是只差几个字段名，而是有三处会直接导致报表金额口径错误：

1. **非金币薪资没有按主播、公会长分别完整输出。**
2. **可提现余额实际上不含金币，而且历史周期读取旧快照，不是当前实时余额。**
3. **公会总薪资直接信任 `total_salary_amount`，没有在后台查询层保证等于“主播总薪资 + 公会长管理收益”。**

---

## 三、后台—主播薪资字段差异

### 3.1 汇总字段

| 1.4 需求字段 | 1.4 需求公式/来源 | 当前代码字段或逻辑 | 差异结论 | 风险 |
|---|---|---|---|---|
| 钻石流水 | `Σ 有效钻石流水` | `SUM(valid_diamond_amount_total)` → `effective_diamond_amount_total` | 基本具备；字段名称需统一为钻石流水 | P2 |
| 钻石余额 | `Σ 有效钻石余额` | `SUM(valid_diamond_balance_amount)` → `valid_diamond_balance_amount_total` | 基本具备 | P2 |
| 兑换钻石流水 | `Σ 已确认兑换记录` | 月结查询兑换日志并对 `ABS(income)` 求和；周结汇总 `week_exchange_diamond` | 有字段，但月结数据来源含后台覆盖逻辑，需确认是否完全等同已确认记录 | P1 |
| 主播非金币薪资 | `Σ 主播工资项 × (1-金币工资比例)` | 汇总接口没有独立 `anchor_non_coin_salary_amount`；仅有主播总薪资、主播金币薪资 | 缺失，不能直接展示 | P0 |
| 主播金币薪资 | `Σ 主播工资项 × 金币工资比例` | `SUM(coin_reward_amount)` → `coin_reward_amount_total` | 基本具备，但比例正确性需联查 | P1 |
| 主播总薪资 | `主播非金币薪资 + 主播金币薪资` | `SUM(anchor_total_salary_amount)` | 有总额，但接口没有用非金币+金币进行一致性校验 | P1 |
| 可提现余额（含金币） | `Σ 当前主播实时可提现余额（含金币）` | 当前汇总返回 `salary_balance_amount_total`，公式为主播总薪资减预支；不是当前余额池实时值 | 字段和公式均不符合 | P0 |
| 公会长非金币分成薪资 | `Σ 管理收益工资项 × (1-金币工资比例)` | 没有独立汇总字段 | 缺失 | P0 |
| 公会长分成金币薪资 | `Σ 管理收益工资项 × 金币工资比例` | `guild_coin_reward_amount_total` | 基本具备，但命名需统一 | P1 |
| 公会长总薪资 | `公会长非金币分成薪资 + 公会长分成金币薪资` | `SUM(owner_share_amount)` → `owner_total_salary_amount` | 有总额，但没有拆分一致性校验；需确认 `owner_share_amount` 是否已经包含金币 | P0 |
| 公会总非金币薪资 | `主播非金币薪资 + 公会长非金币分成薪资` | 无对应主播薪资汇总字段 | 缺失 | P0 |
| 公会总金币薪资 | `主播金币薪资 + 公会长分成金币薪资` | `coin_reward_amount + guild_coin_reward_amount` → `guild_total_coin_salary_amount` | 基本具备 | P1 |
| 公会总薪资 | `主播总薪资 + 公会长总薪资` | `SUM(total_salary_amount)` → `guild_total_salary_amount` | 未显式按需求公式计算；需核对 `total_salary_amount` 实际含义 | P0 |

### 3.2 列表字段

| 1.4 需求字段 | 当前代码 | 差异结论 | 风险 |
|---|---|---|---|
| 公会名称、公会ID | `guild_name`、`guild_no` | 已具备；当前页面使用公会编号而非内部主键，前端需统一展示口径 | P2 |
| 公会长名称、公会长ID | `owner_nickname`、`owner_user_code` | 已具备 | P2 |
| 主播名称、主播ID | `nickname`、`anchor_user_code` | 已具备 | P2 |
| 主播政策 | `policy_type`、`policy_type_text` | 已具备月结/周结枚举入口 | P2 |
| 钻石流水 | `current_valid_diamond_flow_amount` / `valid_diamond_amount_total` | 已具备，但存在两个近义字段，需固定页面取值 | P1 |
| 钻石余额 | `current_valid_diamond_balance_amount` / `valid_diamond_balance_amount` | 已具备，但存在近义字段，需固定页面取值 | P1 |
| 兑换钻石流水 | `exchange_diamond_amount` | 已具备 | P2 |
| 主播非金币薪资 | 无独立字段 | 缺失 | P0 |
| 主播金币薪资 | `coin_reward_amount` | 已具备 | P1 |
| 主播总薪资 | `anchor_total_salary_amount` | 已具备 | P1 |
| 公会长非金币分成薪资 | 无独立字段 | 缺失 | P0 |
| 公会长分成金币薪资 | `guild_coin_reward_amount` | 已具备 | P1 |
| 公会长总薪资 | `owner_share_amount` | 名称与含义不清；需确认其是否为含金币管理收益总额 | P0 |
| 操作 | 详情接口及账单ID | 已具备 | P2 |
| 可提现余额（含金币） | `withdrawable_amount` | 当前只统计个人薪资余额和转入余额，明确排除金币工资余额；不符合字段名称 | P0 |

### 3.3 主播可提现余额公式差异

**1.4 需求：**

```text
主播可提现余额（含金币）
= 当前个人薪资余额池可提现部分
+ 当前金币工资余额池可提现部分
+ 已实际入账且仍符合提现条件的转入薪资余额
```

**当前代码：**

```text
当前周期 withdrawable_amount
= pool_type=1 的 available_amount
+ pool_type=3 的 available_amount
```

代码注释明确写了“排除金币工资奖励余额”，即没有统计 `pool_type=2`。

此外：

```text
当前代码历史周期 withdrawable_amount
= 历史结算快照中的 withdrawable_amount
```

而 1.4 要求无论当前周期还是历史周期，都展示**当前实时余额**。

**整改公式：**

```text
withdrawable_amount_including_coin
= 当前可用的 pool_type=1
+ 当前可用的 pool_type=2
+ 当前可用且符合归属规则的 pool_type=3
```

需要同时满足：

- 只统计 `pool_status=1`；
- 不包含冻结金额；
- 不从周期工资反推；
- 查询历史周期时仍读取当前余额池；
- 前端明确该字段为实时值。

---

## 四、后台—公会薪资字段差异

### 4.1 汇总字段

| 1.4 需求字段 | 1.4 需求公式/来源 | 当前代码字段或逻辑 | 差异结论 | 风险 |
|---|---|---|---|---|
| 公会总数 | 去重统计公会ID | `COUNT(*) as guild_count` | 查询源为每公会一条汇总行时可用；建议改为 `COUNT(DISTINCT guild_id)` 防脏数据重复 | P1 |
| 活跃主播数 | 周期内有效结算主播去重数 | 单独查询主播工资明细并计数 | 基本具备 | P2 |
| 钻石流水 | `Σ 有效钻石流水` | `SUM(valid_diamond_amount_total)` | 基本具备 | P2 |
| 钻石余额 | `Σ 有效钻石余额` | `SUM(valid_diamond_balance_amount)` | 基本具备 | P2 |
| 兑换钻石流水 | `Σ 已确认兑换钻石记录` | 按主播工资明细的月/周兑换钻石汇总 | 有字段，仍需确认月结覆盖记录口径 | P1 |
| 主播非金币薪资 | `Σ 主播工资项非金币部分` | 无独立汇总字段 | 缺失 | P0 |
| 主播金币薪资 | `Σ 主播工资项金币部分` | `SUM(coin_reward_amount)` | 基本具备 | P1 |
| 主播总薪资 | `主播非金币薪资 + 主播金币薪资` | `SUM(anchor_total_salary_amount)` | 有总额，未校验拆分恒等式 | P1 |
| 公会长非金币分成薪资 | `Σ 管理收益工资项非金币部分` | 无独立字段 | 缺失 | P0 |
| 公会长分成金币薪资 | `Σ 管理收益工资项金币部分` | `SUM(guild_coin_reward_amount)` | 基本具备 | P1 |
| 公会长总薪资 | `公会长非金币分成薪资 + 公会长分成金币薪资` | `SUM(owner_share_amount)` | 有总额，但未按拆分字段校验 | P0 |
| 公会总非金币薪资 | `主播非金币薪资 + 公会长非金币分成薪资` | `SUM(non_coin_reward_amount)` | 没有证明该字段同时包含主播和公会长两类非金币工资 | P0 |
| 公会总金币薪资 | `主播金币薪资 + 公会长分成金币薪资` | `SUM(coin_reward_amount + guild_coin_reward_amount)` | 公式基本一致 | P1 |
| 公会总薪资 | `主播总薪资 + 公会长总薪资` | `SUM(total_salary_amount)` | 不是按需求两个分项相加，依赖上游字段语义 | P0 |
| 可提现余额（含金币） | 当前公会可归因实时余额 | 无符合 1.4 定义的统一字段 | 缺失 | P0 |

### 4.2 列表字段

| 1.4 需求字段 | 当前代码 | 差异结论 | 风险 |
|---|---|---|---|
| 公会名称、公会ID | `guild_name`、`guild_no` | 已具备 | P2 |
| 公会长名称、公会长ID | `owner_nickname`、`owner_user_code` | 已具备 | P2 |
| 成员总数 | `member_total_count` | 当前按实时成员关系统计；历史周期没有确认读取成员快照，可能被当前成员变化覆盖 | P1 |
| 兑换钻石流水 | `exchange_diamond_amount` | 已具备 | P2 |
| 钻石流水 | `effective_diamond_amount_total` | 已具备 | P2 |
| 钻石余额 | `valid_diamond_balance_amount_total` | 已具备 | P2 |
| 主播非金币薪资 | 无独立字段 | 缺失 | P0 |
| 主播金币薪资 | `anchor_coin_reward_amount_total` | 已具备 | P1 |
| 主播总薪资 | `anchor_total_salary_amount` | 已具备 | P1 |
| 公会长非金币分成薪资 | 无独立字段 | 缺失 | P0 |
| 公会长分成金币薪资 | `guild_coin_reward_amount_total` | 已具备 | P1 |
| 公会长总薪资 | `owner_total_salary_amount = owner_share_amount` | 需确认是否为包含金币的管理收益总额 | P0 |
| 公会总非金币薪资 | `guild_non_coin_reward_amount_total = non_coin_reward_amount` | 字段存在，但没有按主播非金币+公会长非金币显式计算 | P0 |
| 公会总金币薪资 | `guild_total_coin_salary_amount` | 基本具备 | P1 |
| 公会总薪资 | `guild_total_salary_amount = total_salary_amount` | 未显式按主播总薪资+公会长总薪资计算 | P0 |
| 可提现余额（含金币） | 无对应统一字段 | 缺失 | P0 |
| 操作 | 详情接口 | 已具备 | P2 |

### 4.3 公会总薪资公式差异

**1.4 要求：**

```text
公会总非金币薪资
= 主播非金币薪资
+ 公会长非金币分成薪资

公会总金币薪资
= 主播金币薪资
+ 公会长分成金币薪资

公会总薪资
= 公会总非金币薪资
+ 公会总金币薪资
= 主播总薪资
+ 公会长总薪资
```

其中：

- 公会长总薪资在后台报表中只表示公会长管理收益；
- 公会长本人作为主播的工资已经进入主播总薪资；
- 公会总薪资不能再次加公会长本人主播工资。

**当前代码：**

公会薪资查询源限定为：

```text
bill_owner_type = 2
member_id = 0
source_type = 1
```

列表直接映射：

```text
主播总薪资 = row.anchor_total_salary_amount
公会长总薪资 = row.owner_share_amount
公会总薪资 = row.total_salary_amount
```

汇总直接执行：

```text
SUM(anchor_total_salary_amount)
SUM(owner_share_amount)
SUM(total_salary_amount)
```

当前 `manage-api` 没有执行或校验：

```text
total_salary_amount
是否等于 anchor_total_salary_amount + owner_share_amount
```

因此整改时不能继续只相信 `total_salary_amount`，至少要增加以下一致性校验：

```text
公会总薪资 = 主播总薪资 + 公会长管理收益
公会总非金币薪资 = 主播非金币薪资 + 公会长非金币管理收益
公会总金币薪资 = 主播金币薪资 + 公会长金币管理收益
```

若上游汇总行中的 `total_salary_amount` 只表示公会长收益，当前页面就会把公会长收益错显示成公会总薪资。该问题与此前线上出现的“作为公会长总工资取成公会总工资”属于同一类字段语义混用风险。

---

## 五、工资拆分与政策公式差异

### 5.1 主播与公会长计算基础

**1.4 要求：**

```text
主播工资：基于有效钻石余额
公会长管理收益：基于有效钻石流水
```

`GuildController::calculatePolicySalary()` 当前只接收一个 `$validDiamond`，并基于同一个数值同时计算：

- 主播底薪；
- 主播 TG 奖励；
- 公会长分成。

这不符合“主播按余额、公会长按流水”的双基数要求。

不过，该函数在当前仓库中没有发现明确调用点，月结和实时工资主链路会转交 `room-api`。因此：

- 本地函数属于明确不合规的遗留实现，不能继续复用；
- 线上真实工资公式必须继续核对 `room-api`；
- 后台页面必须分别读取有效钻石流水和有效钻石余额，不得用同一个字段替代。

### 5.2 自由政策参数

| 参数 | 1.4 需求 | 当前 PHP 常量/逻辑 | 结论 |
|---|---:|---:|---|
| 月结政策分成比 | 读取当期政策快照 | 固定 `1.10` | 不符合快照驱动要求 |
| 月结钻石折算美元比例 | 读取当期政策快照 | 固定 `3000` | 不符合快照驱动要求 |
| 月结主播分成比例 | 读取当期政策快照 | 固定 `0.80` | 不符合快照驱动要求 |
| 月结公会长分成比例 | 读取当期政策快照 | 固定 `0.20` | 不符合快照驱动要求 |
| 自由政策金币工资比例 | 读取工资项政策快照 | PHP 默认固定 `0.15` | 与已确认的 0.2 口径存在冲突，需联查 room-api |
| 周结政策分成比 | `0.8` | 当前 PHP 列表只读周表，不在本地计算 | 需联查 room-api |
| 周结钻石折算美元比例 | `130000` | 当前 PHP 未实现该公式 | 需联查 room-api |
| 周结主播分成比例 | `0.73` | 当前 PHP 未实现该公式 | 需联查 room-api |
| 周结公会长分成比例 | `0.27` | 当前 PHP 未实现该公式 | 需联查 room-api |
| 周结金币工资比例 | `0.2` | 当前 PHP 未实现该公式 | 需联查 room-api |

### 5.3 金币与非金币拆分

**1.4 要求：**

```text
先逐工资项拆分，再汇总：
工资项金币工资 = 工资项总额 × 当期金币工资比例
工资项非金币工资 = 工资项总额 × (1 - 当期金币工资比例)
```

当前 PHP 的拆分函数对主播底薪、TG 奖励、公会长分成分别计算金币工资，这个“逐工资项拆分”的方向是对的。

但存在三个问题：

1. 自由政策默认金币工资比例仍是 `0.15`；
2. 普通政策默认金币工资比例为 `0.00`；
3. 对外接口没有完整返回主播非金币、公会长非金币两类结果，导致页面无法按 1.4 字段闭环核对。

### 5.4 TG/非自由政策超档公式

**1.4 要求：**

```text
超档增量
= floor((实际值 - 当前档目标值) ÷ 超档步长)
× 当前档超档金额

总工资基数
= 当前档基础薪资 + 超档增量
```

当前本地函数：

```text
超档增量
= floor((有效钻石 - 档位目标钻石) ÷ over_target_divisor)
```

代码没有单独乘以“当前档超档金额”字段，而是默认每个步长增加 1 个金额单位。

如果政策配置中的超档金额并不恒等于 1 USD，则当前本地公式不符合需求。该函数当前疑似不是线上主计算链路，仍需在 `room-api` 中核对真实实现。

---

## 六、已删除字段与旧逻辑残留

| 1.4 已删除内容 | 当前代码残留 | 影响 |
|---|---|---|
| 预支、借支、待抵扣、对冲 | `advance_deduction_amount`、预支记录查询、工资减预支公式 | 会直接改变主播薪资余额、公会长薪资余额和公会总薪资余额 |
| 起始钻石余额 | `start_diamond_balance_amount` | 接口、汇总和导出仍输出 |
| 最终钻石余额 | `end_diamond_balance_amount` | 接口、汇总和导出仍输出 |
| 公会总薪资余额 | `guild_salary_balance_amount_total` | 仍按公会总薪资减预支计算 |
| 公会长薪资余额 | `owner_salary_balance_amount_total` | 仍按本人主播工资+管理收益-预支计算 |
| 公会总预支 | `guild_advance_amount_total` 等 | 汇总、列表和导出仍保留 |

### 导出仍存在的旧字段

当前公会薪资导出仍包含：

- 起始钻石余额；
- 最终钻石余额；
- 公会总预支；
- 公会总薪资余额；
- 公会长薪资余额。

与 1.4 页面字段不一致，导出需要同步清理，不能只改前端表格。

---

## 七、建议研发采用的统一返回字段

### 7.1 主播薪资行

| 页面字段 | 建议接口字段 | 统一公式/来源 |
|---|---|---|
| 钻石流水 | `valid_diamond_flow_amount` | 周期有效钻石流水 |
| 钻石余额 | `valid_diamond_balance_amount` | 周期有效钻石余额 |
| 兑换钻石流水 | `exchange_diamond_amount` | 周期已确认兑换记录 |
| 主播非金币薪资 | `anchor_non_coin_salary_amount` | 主播工资项非金币部分合计 |
| 主播金币薪资 | `anchor_coin_salary_amount` | 主播工资项金币部分合计 |
| 主播总薪资 | `anchor_total_salary_amount` | 非金币 + 金币 |
| 公会长非金币分成薪资 | `owner_non_coin_share_amount` | 该主播贡献的管理收益非金币部分 |
| 公会长分成金币薪资 | `owner_coin_share_amount` | 该主播贡献的管理收益金币部分 |
| 公会长总薪资 | `owner_total_share_amount` | 公会长非金币分成 + 公会长金币分成 |
| 可提现余额（含金币） | `withdrawable_balance_including_coin` | 当前实时余额池合计 |

### 7.2 公会薪资行

| 页面字段 | 建议接口字段 | 统一公式/来源 |
|---|---|---|
| 主播非金币薪资 | `anchor_non_coin_salary_amount_total` | Σ 主播非金币工资项 |
| 主播金币薪资 | `anchor_coin_salary_amount_total` | Σ 主播金币工资项 |
| 主播总薪资 | `anchor_total_salary_amount` | 主播非金币 + 主播金币 |
| 公会长非金币分成薪资 | `owner_non_coin_share_amount_total` | Σ 公会长非金币管理收益 |
| 公会长分成金币薪资 | `owner_coin_share_amount_total` | Σ 公会长金币管理收益 |
| 公会长总薪资 | `owner_total_share_amount` | 公会长非金币 + 公会长金币 |
| 公会总非金币薪资 | `guild_non_coin_salary_amount` | 主播非金币 + 公会长非金币 |
| 公会总金币薪资 | `guild_coin_salary_amount` | 主播金币 + 公会长金币 |
| 公会总薪资 | `guild_total_salary_amount` | 主播总薪资 + 公会长总薪资 |
| 可提现余额（含金币） | `guild_withdrawable_balance_including_coin` | 当前公会可归因实时余额汇总 |

### 7.3 必须增加的恒等式校验

```text
主播总薪资
= 主播非金币薪资 + 主播金币薪资

公会长总薪资
= 公会长非金币分成薪资 + 公会长分成金币薪资

公会总非金币薪资
= 主播非金币薪资 + 公会长非金币分成薪资

公会总金币薪资
= 主播金币薪资 + 公会长分成金币薪资

公会总薪资
= 公会总非金币薪资 + 公会总金币薪资
= 主播总薪资 + 公会长总薪资
```

任何一条不相等，都不应该静默展示，应记录异常并阻止生成错误汇总。

---

## 八、整改优先级

### P0——开发前必须改

1. 新增主播非金币薪资、公会长非金币分成薪资及其汇总字段。
2. 公会总薪资改为由主播总薪资和公会长管理收益明确相加，不再裸用语义不明的 `total_salary_amount`。
3. 主播可提现余额加入金币工资余额池，并始终读取当前实时余额。
4. 实现公会维度“可提现余额（含金币）”统一字段及去重归属规则。
5. 从主播、公会薪资金额计算中移除预支扣减。
6. 核对 `room-api` 的主播余额基数、公会长流水基数、金币比例 0.2、周结参数和 TG 超档金额公式。

### P1——联调前完成

1. 清理接口和导出的起始/最终钻石余额、预支、公会总薪资余额等旧字段。
2. 历史成员数、历史公会长关系必须读周期快照。
3. 统一兑换钻石流水的“已确认记录”范围，确认后台覆盖记录是否应计入。
4. 修复指定成员实时刷新不接受 `policy_type=3` 的问题。
5. 统一字段命名，避免同一含义同时存在两套字段。

### P2——展示规范

1. 页面、接口、导出统一使用“非金币薪资”。
2. 公会长总薪资明确标注为“公会长管理收益”，不含本人主播工资。
3. 可提现余额明确标注“实时”，避免与查询周期工资混淆。

---

## 九、代码证据索引

| 证据 | 文件及位置 |
|---|---|
| 主播薪资汇总直接 SUM 总额、金币额、预支额 | `GuildController.php:1440-1529` |
| 主播列表字段映射及 `withdrawable_amount` | `GuildController.php:1152-1259` |
| 主播实时余额只统计余额池 1、3 | `GuildController.php:9587-9608` |
| 历史周期可提现余额读取结算快照 | `GuildController.php:9610-9632` |
| 公会薪资只查询公会长汇总行 | `GuildController.php:986-1010` |
| 公会列表直接映射主播、公会长、公会总薪资 | `GuildController.php:8803-8851` |
| 公会顶部汇总直接 SUM 原字段 | `GuildController.php:8857-8911` |
| 公会实时字段仍包含预支和工资余额 | `GuildController.php:9056-9159` |
| 自由政策硬编码 1.10、3000、0.80、0.20 | `GuildController.php:59-68、9307-9322` |
| 金币工资按工资项拆分 | `GuildController.php:9404-9457` |
| 本地 TG 超档及有效天公式 | `GuildController.php:9325-9372` |
| 月结生成和确认实际调用 room-api | `GuildController.php:9920-10041` |
| 指定成员实时刷新仅接受政策 1、2 | `GuildController.php:10104-10152` |
| 月结工资表字段及旧预支字段 | `create_guild_core_tables.php:140-194` |
| 公会薪资导出仍带旧字段 | `GuildController.php:1657-1704` |

---

## 十、最终判断

当前代码已经具备月结/周结列表入口、钻石流水、钻石余额、兑换钻石、主播总薪资、公会长总薪资、金币工资等基础数据。

但它还不能直接满足 1.4 的字段和公式要求，核心原因不是前端少加几列，而是：

- 非金币工资拆分结果没有完整输出；
- 公会总薪资缺少明确的组合公式保证；
- 可提现余额口径与“实时、含金币”相反；
- 已取消的预支仍参与计算；
- 新周结政策的真实计算参数位于外部 `room-api`，当前仓库无法独立完成最终公式验真。

研发应先统一工资项和汇总字段模型，再改页面。否则只在前端用“总额减金币”临时拼字段，后续不同政策、不同金币比例和历史快照一混，账又会他妈地对不上。
